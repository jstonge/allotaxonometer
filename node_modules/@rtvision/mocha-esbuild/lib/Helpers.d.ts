import { CustomMochaOptions, RunMochaOptions } from './types';
/**
 * From: https://github.com/webpack/webpack-cli/blob/0fa244b0/packages/webpack-cli/lib/utils/dynamic-import-loader.js
 *
 * We can't directly call "import" because TypeScript compiles it
 * https://github.com/microsoft/TypeScript/issues/43329
 */
export declare function importESM(specifier: string): any;
export declare function initWorker(bundledFile: string, { mochaOptions, mochaConfigPath }: {
    mochaOptions?: CustomMochaOptions | null;
    mochaConfigPath?: string;
}): void;
export declare function runMocha(filename: string, { mochaOptions, isWorker, mochaConfigPath }?: RunMochaOptions): Promise<number>;
export declare function cleanUp(file: string): Promise<void>;
export declare function resolveRelativePath(filePath: string): string;
