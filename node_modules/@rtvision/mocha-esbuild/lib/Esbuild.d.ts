import { EsbuildFlags } from './types';
export default function (entryPoint: string, outputFile: string, { watch, sourcemap, esbuildConfigPath, onRebuild, importSourceMapSupport }: EsbuildFlags): Promise<import("esbuild").BuildResult>;
